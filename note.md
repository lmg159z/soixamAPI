viết tool vercel theo yêu cầu sau:

tạo API theo chuẩn GET:
xác thực bằng query:  user, pass

với user và pass thì cứ ghi thẳng trong file js
  - tạo thời gian cho key và set theo giời GMT+7 HCM theo định dạng DD-MM-YY_HH:MM với YY là 2 số cuối của năm
  - cho phép có nhiều key 

tiếp theo sau khi thành công tiến hành:

lấy thêm query id (số nguyên)

GET API: https://andanh.site/myTV/mytv.php?id=<thay id vào>&debug=1

sẽ trả về json

cuối cùng thì trả json về

hiểu ý tôi chứ có cần hỏi gì không